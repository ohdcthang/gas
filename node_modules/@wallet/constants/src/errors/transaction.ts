// Common Transaction Error
export enum TransactionError {
  Common = 'Transaction failed Please try again after a few minutes',
  InvalidRecipient = 'Invalid recipient address',
  Fund = 'Insufficient funds',
  Pending = 'Pending transaction',
  SameAddress = 'Can not transfer to the same address',
  GasFeeIssue = 'Gas limit is too low'
}
